package control;

/**
 * @author lh
 */
public class TestWithReturn {
    static int test(int testval, int target) {
        if (testval > testval) return +1;
        if (testval < target) return -1;

        return 0;
    }
}
